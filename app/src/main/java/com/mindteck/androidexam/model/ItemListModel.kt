package com.mindteck.androidexam.model

data class ItemListModel (
    // on below line we are creating a two variable
    // one for course name and other for course image.
    var itemName: String,
    var itemImage: Int
    )